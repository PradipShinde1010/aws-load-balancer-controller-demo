Demo 1: Deploying the App using AWS Load Balancer Controller - Tested
==============================================
Repo URL: https://github.com/KastroVKiran/aws-load-balancer-controller-demo.git

demo1-alb-path-routing/
│
├── app1/
│   ├── app.py
│   ├── requirements.txt
│   └── Dockerfile
│
├── app2/
│   ├── app.py
│   ├── requirements.txt
│   └── Dockerfile
│
├── app3/
│   ├── app.py
│   ├── requirements.txt
│   └── Dockerfile
│
└── k8s/
    ├── app1-deployment.yaml
    ├── app2-deployment.yaml
    ├── app3-deployment.yaml
    ├── app1-service.yaml
    ├── app2-service.yaml
    ├── app3-service.yaml
    └── ingress.yaml

docker build -t kastrov/app1:1.0 ./app1
docker build -t kastrov/app2:1.0 ./app2
docker build -t kastrov/app3:1.0 ./app3

docker push kastrov/app1:1.0
docker push kastrov/app2:1.0
docker push kastrov/app3:1.0

🔹 STEP 1 — OIDC (correct cluster name!)
eksctl utils associate-iam-oidc-provider \
--region ap-south-1 \
--cluster kastro-eks \
--approve

🔹 STEP 2 — IAM Policy (latest)
curl -O https://raw.githubusercontent.com/kubernetes-sigs/aws-load-balancer-controller/main/docs/install/iam_policy.json

aws iam create-policy \
  --policy-name AWSLoadBalancerControllerIAMPolicy-v3 \
  --policy-document file://iam_policy.json

🔹 STEP 3 — IAM Service Account (FIXED cluster name!)
eksctl create iamserviceaccount \
  --cluster kastro-eks \
  --namespace kube-system \
  --name aws-load-balancer-controller \
  --attach-policy-arn arn:aws:iam::560185625463:policy/AWSLoadBalancerControllerIAMPolicy-v3 \
  --approve \
  --region ap-south-1

🔹 STEP 4 — Install Controller
curl https://raw.githubusercontent.com/helm/helm/main/scripts/get-helm-3 | bash
helm version

helm repo add eks https://aws.github.io/eks-charts
helm repo update

helm install aws-load-balancer-controller eks/aws-load-balancer-controller \
  -n kube-system \
  --set clusterName=kastro-eks \
  --set serviceAccount.create=false \
  --set serviceAccount.name=aws-load-balancer-controller \
  --set region=ap-south-1

🔍 Verify:
kubectl get pods -n kube-system

🔹 STEP 5 — Deploy Apps 
kubectl apply -f k8s/


🔹 STEP 6 — Deploy Ingress controller
kubectl apply -f k8s/ingress.yml


🔹 STEP 7 — Wait for ALB
kubectl get ingress -w
You will see LB URL in a while

🌐 STEP 8 — Access the App
✅ These will work:
http://<ALB-DNS>/app1
http://<ALB-DNS>/app2
http://<ALB-DNS>/app3

****************************
CLEAN UP COMMANDS
****************************
🧹 PHASE 1 — Delete Kubernetes Workloads
🔥 Delete Ingress (THIS deletes ALB)
kubectl delete ingress microservices-ingress

👉 Wait ~1–2 minutes (ALB deletion is async)

🔥 Delete Services
kubectl delete svc app1-service app2-service app3-service
🔥 Delete Deployments (Pods go away)
kubectl delete deploy app1 app2 app3
🔥 Delete everything (shortcut if needed)
kubectl delete all --all

🧹 PHASE 2 — Clean AWS Load Balancer Resources
🔍 Verify ALB deletion
kubectl get ingress

👉 Should be empty

🔍 Check AWS Console (IMPORTANT)

Go to:

EC2 → Load Balancers → delete if still exists
EC2 → Target Groups → delete all k8s-*

🧹 PHASE 3 — Delete TargetGroupBindings (VERY IMPORTANT)
kubectl get targetgroupbindings
kubectl delete targetgroupbindings --all

🧹 PHASE 4 — Remove AWS Load Balancer Controller
🔥 Uninstall Helm release
helm uninstall aws-load-balancer-controller -n kube-system
🔍 Verify
kubectl get pods -n kube-system

👉 No controller pods should exist

🧹 PHASE 5 — Delete IAM Service Account (IRSA)
eksctl delete iamserviceaccount \
  --cluster kastro-clusterr \
  --namespace kube-system \
  --name aws-load-balancer-controller \
  --region ap-south-1

🧹 PHASE 6 — Delete IAM Policy (Optional)
aws iam delete-policy \
  --policy-arn arn:aws:iam::560185625463:policy/AWSLoadBalancerControllerIAMPolicy-v3
🧹 PHASE 7 — Delete OIDC Provider (Optional advanced cleanup)

⚠️ Only if you won’t use IRSA anymore:

aws iam list-open-id-connect-providers

Then:

aws iam delete-open-id-connect-provider \
  --open-id-connect-provider-arn <OIDC-ARN>

🧹 PHASE 8 — Delete Entire EKS Cluster (FULL CLEANUP)

👉 If you want complete teardown

eksctl delete cluster \
  --name kastro-clusterr \
  --region ap-south-1
